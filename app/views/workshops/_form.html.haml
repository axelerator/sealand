= simple_form_for(@workshop) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :plan_id, :as => :select, :collection => Plan.all
  .form-actions
    = f.button :submit
