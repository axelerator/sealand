= simple_form_for(@workshop) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :plan_id, :as => :select, :collection => Plan.all
    = f.input :lat
    = f.input :lng
  .form-actions
    = f.button :submit
